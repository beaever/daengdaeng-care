# React Native/Metro 는 평탄한 node_modules 를 기대한다.
# pnpm 기본(isolated) 레이아웃은 전이 의존성(@babel/runtime 등) 해석을 깨뜨려
# Expo 실기기 빌드/번들에 실패하므로, Expo 공식 권장대로 hoisted 링커를 사용한다.
# 참고: https://docs.expo.dev/guides/monorepos/#common-issues
node-linker=hoisted
