{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["app/**", "components/**", "lib/**", "public/**", "next.config.mjs", "tsconfig.json"],
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
